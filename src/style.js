const { StyleSheet } = require('react-native');

const styles = StyleSheet.create({
  modal: {
    flex: 1,
    margin: 0,
    paddingVertical: 0,
  },
  mainView: {
    flex: 1,
  },
  headerView: {
    flex: 0,
  },
});
export default styles;
