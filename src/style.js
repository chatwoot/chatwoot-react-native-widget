const { StyleSheet } = require('react-native');

const styles = StyleSheet.create({
  modal: {
    paddingVertical: 16,
  },
  mainView: {
    flex: 1,
    borderRadius: 16,
  },
});
export default styles;
